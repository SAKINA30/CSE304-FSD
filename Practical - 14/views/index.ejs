<!DOCTYPE html>
<html>
<head>
  <title>Job Portal - Upload Resume</title>
  <style>
    body { font-family: Arial, sans-serif; padding:40px; background:#f9fafb; }
    .card { max-width: 600px; margin:auto; background:#fff; padding:20px; border-radius:10px; box-shadow:0 4px 16px rgba(0,0,0,0.1); }
    h1 { margin-top:0; }
    input[type="file"] { margin:20px 0; }
    button { padding:10px 18px; background:#2563eb; color:#fff; border:none; border-radius:6px; cursor:pointer; }
    .msg { margin-top:12px; color:#b91c1c; font-weight:bold; }
  </style>
</head>
<body>
  <div class="card">
    <h1>Upload Your Resume</h1>
    <form action="/upload" method="POST" enctype="multipart/form-data">
      <input type="file" name="resume" accept="application/pdf" required />
      <br>
      <button type="submit">Upload</button>
    </form>
    <% if (message) { %>
      <div class="msg"><%= message %></div>
    <% } %>
  </div>
</body>
</html>
